# RitualistRecognizer Manual Verification

Contract:

```text
0x29682b24E056dCE515747c9ED34a73Be665a8
```

Recommended upload file:

```text
verification/RitualistRecognizer.standard-json-input.json
```

Use Standard JSON Input if the explorer gives you a choice. The file already contains the source and exact compiler settings.

Verification fields:

```text
Contract name: RitualistRecognizer
Contract path / identifier: src/RitualistRecognizer.sol:RitualistRecognizer
Compiler: v0.8.24+commit.e11b9ed9
Optimization: enabled
Optimizer runs: 200
EVM version: cancun
Constructor arguments: empty / none
License: MIT
```

Fallback file:

```text
verification/RitualistRecognizer.flattened.sol
```

Only use the flattened file if Standard JSON Input is not available. If using flattened source, keep the same compiler, optimizer, runs, and EVM version values above.
